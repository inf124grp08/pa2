<!DOCTYPE html>
<html>
	<head>
		<meta charset="utf-8" />
		<meta name="viewport" content="width=device-width" />
		<title>Maximum Climbing</title>
	</head>
	<body>
    <div class="nav-bar">
      <ul>
        <% Object.keys(categories).forEach(key => { %>
          <% var cat = categories[key]; %>
          <li><a href="category-<%= key %>.html"><%= cat.label %></a></li>
        <% }); %>
      </ul>
	</body>
</html>
